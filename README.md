This is the source for a Rails application built for my Ruby on Rails tutorial on YouTube!

Installation:

```
git clone https://github.com/imkmf/todoapp.git
cd todoapp
bundle install
rails db:create && rails db:migrate
rails server
```
